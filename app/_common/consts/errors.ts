const errors = {
  auth: {
    email: {
      invalid_type_error: "Email is invalid",
      required_error: "Email is required",
      sending_failed: "Sending email failed. Please try again later.",
    },
  },
};

export default errors;
